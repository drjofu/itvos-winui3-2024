<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Winui3Beispiele.Views.FlipViewBeispiel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Winui3Beispiele.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:main="using:Winui3Beispiele"
    xmlns:sys="using:System"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

  <StackPanel>
    <FlipView ItemsSource="{x:Bind DerLaden.Artikel}">
      <FlipView.ItemTemplate>
        <DataTemplate  x:DataType="main:Artikel">
          <StackPanel Orientation="Vertical">
            <TextBlock Text="{x:Bind Artikelnummer}" Width="50" HorizontalAlignment="Left"/>
            <TextBlock Text="{x:Bind Bezeichnung}" Width="100" HorizontalAlignment="Left" FontSize="20"/>
            <TextBlock Text="{x:Bind sys:String.Format('{0:0.00}', Preis)}" Width="100" TextAlignment="Left" HorizontalAlignment="Left"/>
            <!--<TextBox Text="{x:Bind Preis, Mode=TwoWay}"/>-->
          </StackPanel>
        </DataTemplate>

      </FlipView.ItemTemplate>
    </FlipView>
  </StackPanel>
</Page>
